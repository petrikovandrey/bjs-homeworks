class AlarmClock {
    constructor() {
        this.alarms = [],
        this.id
    }

    addClock(timeStart, callBack, id) {
        if (id === "undefind") {
            console.error();
            return;
        }
        //1 найти существующий звонок с id
        //2 добавить в массив звонков новый
    }

    removeClock(id) {
        //удалить звонок из массива
    }

    getCurrentFormattedTime() {
        //today.getTime()
        return
    }

    start() {

    }

    stop() {

    }

    printAlarms() {

    }
    
    clearAlarms() {

    }

}
